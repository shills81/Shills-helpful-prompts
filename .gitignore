# ============================================================
# SECRETS AND CREDENTIALS — never commit these
# ============================================================

# Environment variable files
.env
.env.*
*.env
.envrc
.env.local
.env.development
.env.production
.env.staging
.env.test

# API keys, tokens, credentials
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
credentials.json
credentials.yml
credentials.yaml
secret.json
secrets.json
secrets.yml
secrets.yaml
.secrets
.secrets.*
token.json
tokens.json
auth.json

# AWS
.aws/credentials
.aws/config
aws_credentials
aws-credentials.json

# Google Cloud
*-service-account*.json
*serviceaccount*.json
gcp-credentials.json
application_default_credentials.json

# SSH keys
id_rsa
id_rsa.pub
id_dsa
id_dsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
*.ppk

# Database connection strings / config with passwords
database.yml
database.yaml
db.json

# ============================================================
# CLAUDE CODE — sensitive local config
# ============================================================
.claude/
.credentials.json
stats-cache.json
session-meta/
cache-break-*.diff

# ============================================================
# OS / EDITOR JUNK
# ============================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini
ehthumbs.db

.vscode/settings.json
.vscode/*.code-workspace
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.swp
*.swo
*~

# ============================================================
# PACKAGE MANAGERS — never commit lock files with credentials
# ============================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
.yarn/cache

# ============================================================
# BUILD / TEMP ARTIFACTS
# ============================================================
dist/
build/
out/
*.log
*.tmp
*.bak
*.orig
.cache/

# ============================================================
# PRIVATE NOTES AND DRAFTS
# ============================================================
_private/
_drafts/
_notes/
TODO.private.md
NOTES.private.md

# ============================================================
# PERSONAL INFO — common file names for personal data
# ============================================================
*password*
*passwd*
*credit_card*
*ssn*
*private_key*
*priv_key*
