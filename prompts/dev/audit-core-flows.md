# Audit Core User Flows

**Use case:** Walk through the most critical user flows by reading code and flag broken links, UX dead-ends, and missing states.
**Works with:** Claude (any version)

---

Walk these core flows for [YOUR APP NAME] by reading the code (do not run it):

1. First load — what does the user see, in what order?
2. Navigating to a feature module from the main dashboard
3. Creating a record (form fill + save)
4. Editing and updating an existing record
5. Navigating between modules without losing state
6. [ADD YOUR CRITICAL FLOW — e.g., completing a specific workflow end-to-end]

For each flow:
- List the files touched, in order
- Flag broken links, missing transitions, or dead-ends
- Identify state loss risks between steps

Entry files: [e.g., index.html, dashboard.html, appshell.js]
Module directory: [PATH TO MODULES]
